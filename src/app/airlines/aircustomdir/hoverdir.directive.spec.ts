import { HoverdirDirective } from './hoverdir.directive';

describe('HoverdirDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverdirDirective();
    expect(directive).toBeTruthy();
  });
});
